
class reactions {
  
  String reaction(float T, ArrayList<Element> _Elements) {
    String r = "";
    
    // Gibbs free energy:
    // G = H-TS
    // A reaction will be spontaneous when G is negative
    
    // Get a list of compounds that can be created with these elements.
    
    
    // Find if G is negative for any of them.
    
    return r;
  }
  
  
  
}
